@model IEnumerable<Assessment.DATA.Models.Amigo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table id="myTable" class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AmigoName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AmigoLastN)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AmigoName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AmigoLastN)
            </td>
            <td>
                    <a asp-action="Edit" class="btn btn-warning m-1" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="Details" class="btn btn-primary m-1" asp-route-id="@item.Id">Details</a> |
                    <a asp-action="Delete" class="btn btn-danger m-1" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>





@section Scripts {

    <script>
        let table = new DataTable('#myTable');
    </script>
                }